services:
  - type: web
    name: petution-app
    env: static
    buildCommand: npx vite build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
